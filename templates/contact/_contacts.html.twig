<ul class="contacts">
    {% for contact in contacts %}
        <li>
            <a href="{{ path('app_contact_show', {id: contact.id}) }}">{{contact.id}} -- <span class="firstname">{{ contact.firstname }}</span> -- <span class="lastname">{{ contact.lastname }}</span> -- {{ contact.email }}</a>
        </li>
    {% endfor %}
</ul>